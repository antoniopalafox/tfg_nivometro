#pragma once                    // Le indica al compilador que procese este fichero solo una vez por compilacion

void tasks_start_all(void);     // Crea y lanza todas las tareas de freertos (sensores, publicación, etc.)  
